import 'package:flutter/material.dart';

class ManageDepartment extends StatelessWidget {
  const ManageDepartment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}